create table product (
                        id binary(16),
                        name varchar(255),
                        price int,
                        image_url text,
                        primary key (id)
);