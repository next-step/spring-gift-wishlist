CREATE TABLE productDto (
                         id BIGINT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(255),
                         price INT,
                         image_url VARCHAR(500)
);
